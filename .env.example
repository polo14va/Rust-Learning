# ============================================================================
# VARIABLES DE ENTORNO - Desarrollo Local
# ============================================================================
# Copia este archivo a .env y ajusta los valores según tu entorno
#
# Para desarrollo local (cargo run):
#   - Usa host.docker.internal si Postgres/Redis están en Docker
#   - Usa localhost si están corriendo nativamente
#
# Para Docker Compose:
#   - Usa los nombres de servicio (db, redis)
#
# Para Kubernetes:
#   - Las variables se inyectan desde ConfigMap y Secrets
# ============================================================================

# Base de datos
DATABASE_URL=postgres://postgres:postgres@host.docker.internal:5432/rust_db

# Redis
REDIS_URL=redis://host.docker.internal:6379/

# JWT
JWT_SECRET=dev_secret_key_change_in_production
JWT_EXPIRATION_MINUTES=15

# Rate Limiting
RATE_LIMIT_PER_SECOND=10

# Logging
RUST_LOG=info
